@model HomeViewModel

<partial name="_Carousel" />

<h2 class="h2" >Pies of the week</h2>
<h4 class="h3">Our weekly selection - just for you!</h4>

<div class="row">
    @foreach (var item in Model.PiesOfWeek)
    {
        <partial name="_PieCard" model="item" />
    }
</div>